<h1><%= @title %></h1>
<% @users.each do |user| %>
	<div id="follow_form">
		<p><%= link_to user.email, user %> - <% if current_user.following?(user) %> <%= render 'unfollow', :user => user %> <% else %> <%= render 'follow', :user => user %><% end %> </p>
	</div>
<% end %>