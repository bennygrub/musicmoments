<p id="notice"><%= notice %></p>
<%= render "video"%>

<p>
  <b>Song:</b>
  <%= @moment.song %>
</p>

<p>
  <b>Moment Members:</b>
  <% @moment.users.each do |user| %> <%= user.email %>, <% end %>
</p>
<p>
  <b>Year:</b>
  <%= @moment.year %>
</p>
  
<% if !current_user.nil? %>
  <% if Moment.where("user_id = ? AND id = ?", current_user.id, @moment.id).count > 0 %>
    <%= link_to 'Edit', edit_moment_path(@moment) %> |
  <% end %>
<% end %>
<%= link_to 'Back', current_user %>
